# 2d6mcp Chronicle

Cross-session memory scoped by durable `table_label` (examples: `table-a`, `campaign-label`).

Canon: extracts are always `provisional`. Only `chronicle` action `promote` confirms. `brief` uses no LLM.

See `.claude/skills/2d6mcp-chronicle/SKILL.md` for the full MCP API.
